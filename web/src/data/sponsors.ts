export interface PublicSponsor {
  name: string;
  logoSrc: string;
}

/**
 * Sponsors confirmados para mostrar en la landing.
 * Mantener vacío hasta que exista confirmación real.
 */
export const currentSponsors: PublicSponsor[] = [];
